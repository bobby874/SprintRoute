pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                script {
                    if (isUnix()) {
                        sh 'echo "Building on Unix"'
                    } else {
                        bat 'echo Building on Windows'
                    }
                }
            }
        }
    }
}
